import numpy as np

# Define the size of the array
array_size = 10

# Create an array with random values in the range 1 to 10
random_array = np.random.randn(1, 11, size=array_size)

# Print the resulting array
print(random_array)